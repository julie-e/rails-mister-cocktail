<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :description %>
  <%= f.input :ingredient %>
  <%= f.submit 'dose!', class: "btn btn-success"%>

<% end %>
